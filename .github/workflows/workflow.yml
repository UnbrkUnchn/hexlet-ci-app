name: Lesson_2

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - run: make setup && make start

      - run: make lint

      - run: make test